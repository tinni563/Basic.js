var firendsAge = [15, 17, 14, 16];

var sonaliAge = firendsAge[2];

firendsAge[1] = 21;

var position = firendsAge.indexOf(14);

console.log(firendsAge.length);

firendsAge.push(19);

console.log(firendsAge.length);

firendsAge.pop();

console.log(firendsAge);

var teaLine = ['Palam', 'Kalam', 'Salam', 'Balam', 'Talam'];


teaLine.unshift('Jalam');

console.log(teaLine);

var part = teaLine.slice(2, 5);

console.log(part);

